import { ImageLibraryOptions } from 'react-native-image-picker';

const IMAGE_PICKER_OPTIONS: ImageLibraryOptions = {
  mediaType: 'photo',
  quality: 1,
};


const IMAGE_WIDTH = 300;
const IMAGE_HEIGHT = 500;

export { IMAGE_PICKER_OPTIONS, IMAGE_HEIGHT, IMAGE_WIDTH };
